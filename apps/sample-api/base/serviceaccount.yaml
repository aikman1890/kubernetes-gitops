# Dedicated ServiceAccount with no RBAC bindings — the app doesn't call the
# Kubernetes API, so it gets an identity and nothing else.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sample-api
  labels:
    app.kubernetes.io/name: sample-api
    app.kubernetes.io/managed-by: gitops
automountServiceAccountToken: false
